Examples
--------
Usage of :func:`time_split.split` and :func:`time_split.plot`. The :mod:`.integration` functions do not have examples,
but mostly behave the same way as the :func:`.split`-function.
